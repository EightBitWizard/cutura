// Canonical production origin for absolute SEO URLs (sitemap, hreflang). Override
// at provisioning if the domain differs. CLAUDE.md: cutura.ch / www (Production).
export const SITE_URL = "https://www.cutura.ch";
